# Боевое исполнение на Polymarket. Ставится ОТДЕЛЬНО от основного рантайма:
# без этого пакета проект работает, но ордера на биржу отправить не может —
# app/services/live_execution.py сообщит об этом понятной ошибкой.
#
#   pip install -r requirements-live.txt
#
# Пакет подписывает ордера приватным ключом. Ключи берутся только из окружения
# (CODEX_POLY_PRIVATE_KEY / CLAUDE_POLY_PRIVATE_KEY) и никогда не хранятся
# в репозитории, БД, логах и экспортах.
py-clob-client-v2>=0.1.0
