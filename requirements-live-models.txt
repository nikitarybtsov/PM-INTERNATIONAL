# Опциональные SDK для реальных вызовов LLM-участников.
# Проект полностью работает БЕЗ них (mock-режим).
# Установка: pip install -r requirements-live-models.txt
-r requirements.txt
openai==1.59.6
anthropic==0.42.0
